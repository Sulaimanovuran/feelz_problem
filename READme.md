1. Для запуска создай виртуальное окружение
`python3 -m venv venv`

2. Активируй виртуальное окружение
`source venv/bin/activate`

3. Установи зависимости
`pip install -r requirements.txt` или `pip3 install -r requirements.txt`

4. Запусти бота
`python3 main.py`